import{_ as n,o as t,c as o,af as i,j as s,a as e}from"./chunks/framework.DZTH9Z-c.js";const u=JSON.parse('{"title":"Гайд по действиям системы","description":"Полный справочник всех доступных действий в Coreness. Отправка сообщений, работа с AI, HTTP запросы, валидация, оплаты и многое другое.","frontmatter":{"title":"Гайд по действиям системы","description":"Полный справочник всех доступных действий в Coreness. Отправка сообщений, работа с AI, HTTP запросы, валидация, оплаты и многое другое.","keywords":"действия coreness, send_message, completion, validate, telegram api, AI действия","head":[["meta",{"property":"og:title","content":"Гайд по действиям системы | Coreness"}],["meta",{"property":"og:description","content":"Полный справочник всех доступных действий в Coreness. Отправка сообщений, работа с AI, HTTP запросы, валидация, оплаты и многое другое."}],["meta",{"property":"og:image","content":"https://docs.coreness.tech/logo-dark.png"}],["meta",{"property":"og:url","content":"https://docs.coreness.tech/reference/ACTION_GUIDE"}],["link",{"rel":"canonical","href":"https://docs.coreness.tech/reference/ACTION_GUIDE"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"TechArticle\\",\\"headline\\":\\"Гайд по действиям системы\\",\\"description\\":\\"Полный справочник всех доступных действий в Coreness. Отправка сообщений, работа с AI, HTTP запросы, валидация, оплаты и многое другое.\\",\\"url\\":\\"https://docs.coreness.tech/reference/ACTION_GUIDE\\"}"]]},"headers":[],"relativePath":"reference/ACTION_GUIDE.md","filePath":"reference/ACTION_GUIDE.md"}'),l={name:"reference/ACTION_GUIDE.md"};function d(c,a,r,p,h,g){return t(),o("div",null,[...a[0]||(a[0]=[i(`<h1 id="🎯-гаид-по-деиствиям" tabindex="-1">🎯 Гайд по действиям <a class="header-anchor" href="#🎯-гаид-по-деиствиям" aria-label="Permalink to &quot;🎯 Гайд по действиям&quot;">​</a></h1><p>Полное описание всех доступных действий с их параметрами и результатами.</p><h2 id="📋-содержание" tabindex="-1">📋 Содержание <a class="header-anchor" href="#📋-содержание" aria-label="Permalink to &quot;📋 Содержание&quot;">​</a></h2><ul><li><a href="#ai_rag_service">ai_rag_service</a> (4 действий) <ul><li><a href="#delete_embedding">⭐ delete_embedding</a></li><li><a href="#get_recent_chunks">⭐ get_recent_chunks</a></li><li><a href="#save_embedding">⭐ save_embedding</a></li><li><a href="#search_embedding">⭐ search_embedding</a></li></ul></li><li><a href="#ai_service">ai_service</a> (2 действий) <ul><li><a href="#completion">completion</a></li><li><a href="#embedding">embedding</a></li></ul></li><li><a href="#download_service">download_service</a> (1 действий) <ul><li><a href="#download_and_extract">⭐ download_and_extract</a></li></ul></li><li><a href="#invoice_service">invoice_service</a> (7 действий) <ul><li><a href="#cancel_invoice">cancel_invoice</a></li><li><a href="#confirm_payment">confirm_payment</a></li><li><a href="#create_invoice">create_invoice</a></li><li><a href="#get_invoice">get_invoice</a></li><li><a href="#get_user_invoices">get_user_invoices</a></li><li><a href="#mark_invoice_as_paid">mark_invoice_as_paid</a></li><li><a href="#reject_payment">reject_payment</a></li></ul></li><li><a href="#scenario_helper">scenario_helper</a> (9 действий) <ul><li><a href="#check_value_in_array">check_value_in_array</a></li><li><a href="#choose_from_array">choose_from_array</a></li><li><a href="#format_data_to_text">format_data_to_text</a></li><li><a href="#generate_array">generate_array</a></li><li><a href="#generate_int">generate_int</a></li><li><a href="#generate_unique_id">generate_unique_id</a></li><li><a href="#modify_array">modify_array</a></li><li><a href="#set_cache">set_cache</a></li><li><a href="#sleep">sleep</a></li></ul></li><li><a href="#scenario_processor">scenario_processor</a> (2 действий) <ul><li><a href="#execute_scenario">execute_scenario</a></li><li><a href="#wait_for_action">wait_for_action</a></li></ul></li><li><a href="#storage_hub">storage_hub</a> (4 действий) <ul><li><a href="#delete_storage">delete_storage</a></li><li><a href="#get_storage">get_storage</a></li><li><a href="#get_storage_groups">get_storage_groups</a></li><li><a href="#set_storage">set_storage</a></li></ul></li><li><a href="#telegram_bot_api">telegram_bot_api</a> (5 действий) <ul><li><a href="#answer_callback_query">answer_callback_query</a></li><li><a href="#build_keyboard">build_keyboard</a></li><li><a href="#delete_message">delete_message</a></li><li><a href="#restrict_chat_member">restrict_chat_member</a></li><li><a href="#send_message">send_message</a></li></ul></li><li><a href="#user_hub">user_hub</a> (8 действий) <ul><li><a href="#clear_user_state">clear_user_state</a></li><li><a href="#delete_user_storage">delete_user_storage</a></li><li><a href="#get_tenant_users">get_tenant_users</a></li><li><a href="#get_user_state">get_user_state</a></li><li><a href="#get_user_storage">get_user_storage</a></li><li><a href="#get_users_by_storage_value">get_users_by_storage_value</a></li><li><a href="#set_user_state">set_user_state</a></li><li><a href="#set_user_storage">set_user_storage</a></li></ul></li><li><a href="#validator">validator</a> (1 действий) <ul><li><a href="#validate">validate</a></li></ul></li></ul><p><sup>⭐ — расширение (дополнительный плагин). Для получения информации свяжитесь с <a href="https://t.me/vensus137" target="_blank" rel="noreferrer">разработчиком</a>.</sup></p><p><a id="ai_rag_service"></a></p><h2 id="ai-rag-service" tabindex="-1">ai_rag_service <a class="header-anchor" href="#ai-rag-service" aria-label="Permalink to &quot;ai_rag_service&quot;">​</a></h2><p><strong>Описание:</strong> RAG-расширение для AI Service (векторный поиск и управление embeddings)</p><p><a id="delete_embedding"></a></p><h3 id="⭐-delete-embedding" tabindex="-1">⭐ delete_embedding <a class="header-anchor" href="#⭐-delete-embedding" aria-label="Permalink to &quot;⭐ delete_embedding&quot;">​</a></h3><p><strong>Описание:</strong> Удаление данных из vector_storage по document_id или по дате processed_at</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта (обязательно)</li><li><strong><code>document_id</code></strong> (<code>string</code>, опционально) — ID документа для удаления (удаляет все чанки документа). Если указан, остальные параметры игнорируются</li><li><strong><code>until_date</code></strong> (<code>string</code>, опционально) — Удалить чанки с processed_at &lt;= until_date включительно (формат: &#39;YYYY-MM-DD&#39; или &#39;YYYY-MM-DD HH:MM:SS&#39;). Можно использовать вместе с since_date</li><li><strong><code>since_date</code></strong> (<code>string</code>, опционально) — Удалить чанки с processed_at &gt;= since_date включительно (формат: &#39;YYYY-MM-DD&#39; или &#39;YYYY-MM-DD HH:MM:SS&#39;). Можно использовать вместе с until_date</li><li><strong><code>metadata_filter</code></strong> (<code>object</code>, опционально) — Фильтр по метаданным (JSON объект): например, {&#39;chat_id&#39;: 123, &#39;username&#39;: &#39;user1&#39;}. Используется для фильтрации чанков по сохраненным метаданным. Можно использовать вместе с until_date/since_date</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Информация об ошибке <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>deleted_chunks_count</code></strong> (<code>integer</code>) — Количество удаленных чанков</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;delete_embedding&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # document_id: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # until_date: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # since_date: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # metadata_filter: object (опционально)</span></span></code></pre></div><details><summary>📖 Дополнительная информация</summary><p><strong>Удаление данных из vector_storage:</strong></p><p><strong>Способы удаления:</strong></p><ol><li>По <code>document_id</code> - удаляет все чанки документа (остальные параметры игнорируются)</li><li>По дате <code>processed_at</code> - удаляет чанки по дате обработки</li><li>По <code>metadata_filter</code> - удаляет чанки с определенными метаданными</li></ol><p><strong>Параметры даты:</strong></p><ul><li><code>since_date</code>: удалить с даты (включительно)</li><li><code>until_date</code>: удалить до даты (включительно)</li><li>Можно указать оба параметра (диапазон)</li><li>Формат: &#39;YYYY-MM-DD&#39; или &#39;YYYY-MM-DD HH:MM:SS&#39;</li></ul><p><strong>Примеры:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># По document_id</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  document_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;doc_12345&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># По дате</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  until_date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2024-01-01&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Диапазон дат</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  since_date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2024-01-01&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  until_date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2024-12-31&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># По метаданным</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  metadata_filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">chat_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></details><p><a id="get_recent_chunks"></a></p><h3 id="⭐-get-recent-chunks" tabindex="-1">⭐ get_recent_chunks <a class="header-anchor" href="#⭐-get-recent-chunks" aria-label="Permalink to &quot;⭐ get_recent_chunks&quot;">​</a></h3><p><strong>Описание:</strong> Получение последних N чанков по дате created_at (не векторный поиск, просто выборка по дате, сортировка по created_at для правильного порядка истории)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта (обязательно)</li><li><strong><code>limit_chunks</code></strong> (<code>integer</code>, опционально, диапазон: 1-200) — Количество чанков (по умолчанию 10 из настроек search_limit_chunks). Работает вместе с limit_chars - сначала выбирается limit_chunks чанков, затем фильтруются по limit_chars</li><li><strong><code>limit_chars</code></strong> (<code>integer</code>, опционально, мин: 1) — Лимит по символам (опционально, без ограничения по умолчанию). Работает поверх limit_chunks - после получения limit_chunks чанков они отбираются по порядку (от новых к старым), пока сумма символов не превысит limit_chars. Возвращается столько чанков, сколько влезает в лимит</li><li><strong><code>document_type</code></strong> (<code>string|array</code>, опционально) — Фильтр по типу документа. Строка для одного типа или массив для нескольких типов (например, [&#39;message&#39;, &#39;document&#39;])</li><li><strong><code>document_id</code></strong> (<code>string|array</code>, опционально) — Фильтр по document_id. Строка для одного документа или массив для нескольких (например, [&#39;doc_123&#39;, &#39;doc_456&#39;])</li><li><strong><code>until_date</code></strong> (<code>string</code>, опционально) — Фильтр: искать только чанки с processed_at &lt;= until_date включительно (формат: &#39;YYYY-MM-DD&#39; или &#39;YYYY-MM-DD HH:MM:SS&#39;). Можно использовать вместе с since_date</li><li><strong><code>since_date</code></strong> (<code>string</code>, опционально) — Фильтр: искать только чанки с processed_at &gt;= since_date включительно (формат: &#39;YYYY-MM-DD&#39; или &#39;YYYY-MM-DD HH:MM:SS&#39;). Можно использовать вместе с until_date</li><li><strong><code>metadata_filter</code></strong> (<code>object</code>, опционально) — Фильтр по метаданным (JSON объект): например, {&#39;chat_id&#39;: 123, &#39;username&#39;: &#39;user1&#39;}. Используется для фильтрации чанков по сохраненным метаданным. Можно комбинировать с другими фильтрами</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Информация об ошибке <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>chunks</code></strong> (<code>array (of object)</code>) — Массив найденных чанков (отсортированы по created_at DESC - новые первыми)</li><li><strong><code>chunks_count</code></strong> (<code>integer</code>) — Количество найденных чанков</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get_recent_chunks&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # limit_chunks: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # limit_chars: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # document_type: string|array (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # document_id: string|array (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # until_date: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # since_date: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # metadata_filter: object (опционально)</span></span></code></pre></div><details><summary>📖 Дополнительная информация</summary><p><strong>Получение последних чанков по дате (НЕ векторный поиск):</strong></p><ul><li>Возвращает последние N чанков, отсортированных по <code>created_at</code> (новые первыми, для правильного порядка истории)</li><li>Полезно для получения последних сообщений или истории диалога</li></ul><p><strong>Параметры:</strong></p><ul><li><code>limit_chunks</code>: количество чанков (по умолчанию 10)</li><li><code>limit_chars</code>: лимит по символам (работает поверх <code>limit_chunks</code>)</li></ul><p><strong>Фильтры (комбинируются):</strong></p><ul><li><code>document_type</code>, <code>document_id</code>: строка или массив</li><li><code>since_date</code>, <code>until_date</code>: диапазон дат (формат: &#39;YYYY-MM-DD&#39; или &#39;YYYY-MM-DD HH:MM:SS&#39;)</li><li><code>metadata_filter</code>: JSON объект (например, <code>{chat_id: 123}</code>)</li></ul><p><strong>Примеры:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Последние 10 чанков</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  limit_chunks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Последние сообщения</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  document_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;message&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  limit_chunks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Последние чанки документа</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  document_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;doc_123&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  limit_chunks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Последние чанки за период</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  since_date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2024-01-01&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  until_date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2024-12-31&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  limit_chunks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span></span></code></pre></div></details><p><a id="save_embedding"></a></p><h3 id="⭐-save-embedding" tabindex="-1">⭐ save_embedding <a class="header-anchor" href="#⭐-save-embedding" aria-label="Permalink to &quot;⭐ save_embedding&quot;">​</a></h3><p><strong>Описание:</strong> Сохранение текста в vector_storage с автоматическим разбиением на чанки и генерацией embeddings. Поддерживает сохранение только текста без эмбеддинга через параметр generate_embedding=false</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта (обязательно)</li><li><strong><code>text</code></strong> (<code>string</code>) — Текст для сохранения (будет очищен и разбит на чанки)</li><li><strong><code>document_id</code></strong> (<code>string</code>, опционально) — Уникальный ID документа. Если не указан - будет сгенерирован автоматически через IdSequence (детерминированный по seed)</li><li><strong><code>document_type</code></strong> (<code>string</code>, обязательное, значения: [<code>knowledge</code>, <code>chat_history</code>, <code>other</code>]) — Тип документа: knowledge (база знаний), chat_history (история диалога), other (другое - добавляется в ДОП. КОНТЕКСТ)</li><li><strong><code>role</code></strong> (<code>string</code>, опционально, значения: [<code>system</code>, <code>user</code>, <code>assistant</code>]) — Роль для OpenAI messages (по умолчанию &#39;user&#39;). Используется при формировании контекста в completion</li><li><strong><code>chunk_metadata</code></strong> (<code>object</code>, опционально) — Метаданные чанка (JSON объект): chat_id, username и др. Сохраняется в chunk_metadata и используется для фильтрации при поиске и удалении. Может быть использовано в chunk_format для отображения в контексте AI</li><li><strong><code>model</code></strong> (<code>string</code>, опционально) — Модель для генерации embedding (по умолчанию из настроек ai_client.default_embedding_model)</li><li><strong><code>dimensions</code></strong> (<code>integer</code>, опционально) — Размерность embedding (по умолчанию 1024)</li><li><strong><code>chunk_size</code></strong> (<code>integer</code>, опционально, диапазон: 100-8000) — Размер чанка в символах (по умолчанию 1000, ~300-400 токенов). Адаптивно увеличивается для таблиц и структурированного контента</li><li><strong><code>chunk_overlap</code></strong> (<code>integer</code>, опционально, диапазон: 0-1000) — Перекрытие между чанками в символах (по умолчанию 200, ~20% от chunk_size). Сохраняет контекст на границах чанков для лучшего поиска</li><li><strong><code>replace_existing</code></strong> (<code>boolean</code>, опционально) — Заменить существующий документ (по умолчанию false - добавить). Если документ существует и replace_existing=false - вернется ошибка ALREADY_EXISTS</li><li><strong><code>generate_embedding</code></strong> (<code>boolean</code>, опционально) — Генерировать ли embedding для чанков (по умолчанию true). Если false - сохраняется только текст без эмбеддинга (полезно для истории без векторного поиска)</li><li><strong><code>created_at</code></strong> (<code>string</code>, опционально) — Реальная дата создания (для правильной сортировки истории). Поддерживает форматы: ISO, YYYY-MM-DD, YYYY-MM-DD HH:MM:SS. Если не указано - используется текущее локальное время</li><li>🔑 <strong><code>ai_token</code></strong> (<code>string</code>, опционально) — AI API ключ из конфига тенанта (_config.ai_token). Требуется только если generate_embedding=true</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки: ALREADY_EXISTS (документ существует), INTERNAL_ERROR, VALIDATION_ERROR</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки (например, ошибки валидации полей)</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>document_id</code></strong> (<code>string</code>) — ID сохраненного документа (переданный или сгенерированный)</li><li><strong><code>document_type</code></strong> (<code>string</code>) — Тип документа</li><li><strong><code>chunks_count</code></strong> (<code>integer</code>) — Количество созданных чанков</li><li><strong><code>model</code></strong> (<code>string</code>) (опционально) — Использованная модель embedding (только если generate_embedding=true)</li><li><strong><code>dimensions</code></strong> (<code>integer</code>) (опционально) — Размерность embedding (только если generate_embedding=true)</li><li><strong><code>total_tokens</code></strong> (<code>integer</code>) (опционально) — Общее количество токенов во всех чанках (только если generate_embedding=true)</li><li><strong><code>text_length</code></strong> (<code>integer</code>) — Длина исходного текста (после очистки)</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔑 — поле, которое автоматически берется из конфигурации тенанта (_config) и не требует явной передачи в params</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;save_embedding&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # document_id: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    document_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # role: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # chunk_metadata: object (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # model: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # dimensions: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # chunk_size: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # chunk_overlap: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # replace_existing: boolean (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # generate_embedding: boolean (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # created_at: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # ai_token: string (опционально)</span></span></code></pre></div><details><summary>📖 Дополнительная информация</summary><p><strong>Автоматическое сохранение текста с разбиением на чанки:</strong></p><ol><li>Очистка текста (лишние пробелы, невидимые символы)</li><li>Разбиение на чанки (гибридный подход: по предложениям/абзацам → по символам)</li><li>Генерация embeddings (параллельно, если <code>generate_embedding=true</code>)</li><li>Сохранение в <code>vector_storage</code></li></ol><p><strong>Режимы сохранения:</strong></p><ul><li><code>generate_embedding=true</code> (по умолчанию): с эмбеддингами для векторного поиска</li><li><code>generate_embedding=false</code>: только текст без эмбеддингов (экономит токены, не требует <code>ai_token</code>)</li></ul><p><strong>Перекрытие чанков (chunk_overlap):</strong></p><ul><li>Сохраняет контекст на границах для лучшего поиска</li><li>Рекомендуется ~20% от <code>chunk_size</code> (по умолчанию 100 при <code>chunk_size=512</code>)</li><li>Расширяется до целых предложений при разбиении</li></ul><p><strong>Генерация document_id:</strong></p><ul><li>Если не указан → генерируется автоматически (детерминированный)</li><li>Seed: <code>{tenant_id}:{document_type}:{MD5(text)}</code></li><li>Повторная загрузка того же текста вернет тот же ID</li></ul><p><strong>Обработка существующих документов:</strong></p><ul><li><code>replace_existing=false</code> (по умолчанию): ошибка ALREADY_EXISTS если документ существует</li><li><code>replace_existing=true</code>: старые чанки удаляются, создаются новые</li></ul><p><strong>Пример:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">save_embedding</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Длинный текст...&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  document_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;knowledge&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  chunk_size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">512</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  chunk_overlap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Ответ:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">success</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">response_data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  document_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;doc_12345&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  chunks_count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  total_tokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">150</span></span></code></pre></div></details><p><a id="search_embedding"></a></p><h3 id="⭐-search-embedding" tabindex="-1">⭐ search_embedding <a class="header-anchor" href="#⭐-search-embedding" aria-label="Permalink to &quot;⭐ search_embedding&quot;">​</a></h3><p><strong>Описание:</strong> Поиск похожих чанков по тексту или вектору (semantic search) через cosine similarity</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта (обязательно)</li><li><strong><code>query_text</code></strong> (<code>string</code>, опционально) — Текст запроса для поиска (будет преобразован в embedding). Необходимо указать либо query_text, либо query_vector</li><li><strong><code>query_vector</code></strong> (<code>array</code>, опционально) — Готовый вектор для поиска (массив чисел). Размерность должна совпадать с сохраненными embeddings. Необходимо указать либо query_text, либо query_vector</li><li><strong><code>limit_chunks</code></strong> (<code>integer</code>, опционально, диапазон: 1-200) — Количество результатов (top-k, по умолчанию 10). Работает вместе с limit_chars - сначала выбирается limit_chunks чанков, затем фильтруются по limit_chars</li><li><strong><code>limit_chars</code></strong> (<code>integer</code>, опционально, мин: 1) — Лимит по символам (опционально, без ограничения по умолчанию). Работает поверх limit_chunks - после получения limit_chunks чанков они отбираются по порядку (от большего similarity), пока сумма символов не превысит limit_chars. Возвращается столько чанков, сколько влезает в лимит</li><li><strong><code>min_similarity</code></strong> (<code>number</code>, опционально, диапазон: 0.0-1.0) — Минимальный порог схожести (cosine similarity, по умолчанию из настроек search_min_similarity)</li><li><strong><code>document_id</code></strong> (<code>string|array</code>, опционально) — Фильтр по document_id. Строка для одного документа или массив для нескольких (например, [&#39;doc_123&#39;, &#39;doc_456&#39;])</li><li><strong><code>document_type</code></strong> (<code>string|array</code>, опционально) — Фильтр по типу документа. Строка для одного типа или массив для нескольких типов (например, [&#39;message&#39;, &#39;document&#39;])</li><li><strong><code>until_date</code></strong> (<code>string</code>, опционально) — Фильтр: искать только чанки с processed_at &lt;= until_date включительно (формат: &#39;YYYY-MM-DD&#39; или &#39;YYYY-MM-DD HH:MM:SS&#39;). Можно использовать вместе с since_date</li><li><strong><code>since_date</code></strong> (<code>string</code>, опционально) — Фильтр: искать только чанки с processed_at &gt;= since_date включительно (формат: &#39;YYYY-MM-DD&#39; или &#39;YYYY-MM-DD HH:MM:SS&#39;). Можно использовать вместе с until_date</li><li><strong><code>metadata_filter</code></strong> (<code>object</code>, опционально) — Фильтр по метаданным (JSON объект): например, {&#39;chat_id&#39;: 123, &#39;username&#39;: &#39;user1&#39;}. Используется для фильтрации чанков по сохраненным метаданным. Можно комбинировать с другими фильтрами</li><li><strong><code>model</code></strong> (<code>string</code>, опционально) — Модель для генерации embedding (используется только если указан query_text, по умолчанию из настроек ai_client.default_embedding_model)</li><li><strong><code>dimensions</code></strong> (<code>integer</code>, опционально) — Размерность embedding (используется только если указан query_text, по умолчанию 1024)</li><li>🔑 <strong><code>ai_token</code></strong> (<code>string</code>, опционально) — AI API ключ из конфига тенанта (_config.ai_token). Требуется только если указан query_text</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Информация об ошибке <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>chunks</code></strong> (<code>array (of object)</code>) — Массив найденных чанков</li><li><strong><code>chunks_count</code></strong> (<code>integer</code>) — Количество найденных чанков</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔑 — поле, которое автоматически берется из конфигурации тенанта (_config) и не требует явной передачи в params</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;search_embedding&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # query_text: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # query_vector: array (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # limit_chunks: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # limit_chars: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # min_similarity: number (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # document_id: string|array (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # document_type: string|array (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # until_date: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # since_date: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # metadata_filter: object (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # model: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # dimensions: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # ai_token: string (опционально)</span></span></code></pre></div><details><summary>📖 Дополнительная информация</summary><p><strong>Семантический поиск похожих чанков:</strong></p><ul><li>Поиск по cosine similarity между векторами</li><li>Использует HNSW индекс для быстрого поиска</li><li>Результаты сортируются по убыванию similarity</li></ul><p><strong>Запрос (требуется одно из):</strong></p><ul><li><code>query_text</code>: текст (генерируется embedding автоматически)</li><li><code>query_vector</code>: готовый вектор (не требует API ключ)</li></ul><p><strong>Параметры поиска:</strong></p><ul><li><code>limit_chunks</code>: top-k результатов (по умолчанию 10)</li><li><code>limit_chars</code>: лимит по символам (работает поверх <code>limit_chunks</code>)</li><li><code>min_similarity</code>: минимальный порог (0.0-1.0, по умолчанию 0.7)</li></ul><p><strong>Фильтры (комбинируются):</strong></p><ul><li><code>document_type</code>, <code>document_id</code>: строка или массив</li><li><code>since_date</code>, <code>until_date</code>: диапазон дат (формат: &#39;YYYY-MM-DD&#39; или &#39;YYYY-MM-DD HH:MM:SS&#39;)</li><li><code>metadata_filter</code>: JSON объект (например, <code>{chat_id: 123}</code>)</li></ul><p><strong>Примеры:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Поиск по тексту</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  query_text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Как работает RAG?&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  limit_chunks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  min_similarity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.75</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Поиск с фильтрами</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  query_text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Вопрос&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  document_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;document&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  since_date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2024-01-01&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  until_date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2024-12-31&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Поиск с лимитом по символам</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  query_text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Вопрос&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  limit_chunks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  limit_chars</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5000</span></span></code></pre></div></details><p><a id="ai_service"></a></p><h2 id="ai-service" tabindex="-1">ai_service <a class="header-anchor" href="#ai-service" aria-label="Permalink to &quot;ai_service&quot;">​</a></h2><p><strong>Описание:</strong> Сервис для интеграции ИИ в сценарии</p><p><a id="completion"></a></p><h3 id="completion" tabindex="-1">completion <a class="header-anchor" href="#completion" aria-label="Permalink to &quot;completion&quot;">​</a></h3><p><strong>Описание:</strong> AI completion через ИИ</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>prompt</code></strong> (<code>string</code>) — Текст запроса пользователя</li><li><strong><code>system_prompt</code></strong> (<code>string</code>, опционально) — Системный промпт для контекста</li><li><strong><code>model</code></strong> (<code>string</code>, опционально) — Модель AI (по умолчанию из настроек)</li><li><strong><code>max_tokens</code></strong> (<code>integer</code>, опционально) — Максимальное количество токенов (по умолчанию из настроек)</li><li><strong><code>temperature</code></strong> (<code>float</code>, опционально, диапазон: 0.0-2.0) — Температура генерации (по умолчанию из настроек)</li><li><strong><code>context</code></strong> (<code>string</code>, опционально) — Кастомный контекст (добавляется в финальное user сообщение в блок ДОП. КОНТЕКСТ вместе с other чанками из rag_chunks)</li><li><strong><code>rag_chunks</code></strong> (<code>array (of object)</code>, опционально) — Массив чанков из RAG поиска для автоматического формирования messages. Чанки группируются по типам: chat_history (диалог), knowledge (база знаний), other (другое - добавляется в ДОП. КОНТЕКСТ). Формат: [{content, document_type, role, processed_at, ...}]</li><li><strong><code>json_mode</code></strong> (<code>string</code>, опционально, значения: [<code>json_object</code>, <code>json_schema</code>]) — Режим JSON для структурированного ответа: &#39;json_object&#39; или &#39;json_schema&#39;</li><li><strong><code>json_schema</code></strong> (<code>object</code>, опционально) — JSON схема для режима json_schema (обязательна при json_mode=&#39;json_schema&#39;)</li><li><strong><code>tools</code></strong> (<code>array (of object)</code>, опционально) — Массив объектов-инструментов (tool calling). Каждый элемент — объект: type &#39;function&#39;, function: { name, description, parameters }. parameters — JSON Schema объекта аргументов вызова (OpenAI-совместимый формат).</li><li><strong><code>tool_choice</code></strong> (<code>string</code>, опционально) — Управление выбором инструментов. Строка: &#39;none&#39; (не вызывать), &#39;auto&#39; (по умолчанию при наличии tools), &#39;required&#39; (обязательно вызвать хотя бы один). Объект: принудительный вызов одной функции — {&quot;type&quot;: &quot;function&quot;, &quot;function&quot;: {&quot;name&quot;: &quot;имя_функции&quot;}}.</li><li><strong><code>chunk_format</code></strong> (<code>object</code>, опционально) — Формат отображения чанков в контексте. Шаблоны используют маркеры $ для подстановки значений: $content (обязательно) + любые поля из chunk_metadata. Поддерживается модификатор fallback: $field|fallback:значение. Маркеры работают только с данными чанка (content + chunk_metadata), не затрагивая общий контекст.</li><li>🔑 <strong><code>ai_token</code></strong> (<code>string</code>) — AI API ключ из конфига тенанта (_config.ai_token)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error, timeout</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки (например, ошибки валидации полей)</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>response_completion</code></strong> (<code>string</code>) — Completion ответ от ИИ</li><li><strong><code>prompt_tokens</code></strong> (<code>integer</code>) — Токены на вход (prompt + context)</li><li><strong><code>completion_tokens</code></strong> (<code>integer</code>) — Токены на выход (сгенерированный ответ)</li><li><strong><code>total_tokens</code></strong> (<code>integer</code>) — Общее количество токенов (prompt + completion)</li><li><strong><code>model</code></strong> (<code>string</code>) — Использованная модель</li><li><strong><code>response_dict</code></strong> (<code>object</code>) (опционально) — Распарсенный словарь из JSON ответа (только при использовании json_mode)</li><li><strong><code>tool_calls</code></strong> (<code>array (of object)</code>) (опционально) — При использовании tools — массив объектов вызовов. Каждый элемент: id, type, function: { name, arguments }. arguments — JSON-строка с параметрами вызова (разный набор полей у каждой функции); при необходимости распарсить в сценарии.</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔑 — поле, которое автоматически берется из конфигурации тенанта (_config) и не требует явной передачи в params</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;completion&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # system_prompt: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # model: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # max_tokens: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # temperature: float (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # context: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # rag_chunks: array (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # json_mode: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # json_schema: object (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # tools: array (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # tool_choice: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # chunk_format: object (опционально)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    ai_token</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span></code></pre></div><details><summary>📖 Дополнительная информация</summary><p><strong>JSON режимы:</strong></p><ul><li><code>json_object</code>: модель возвращает валидный JSON (парсится в <code>response_dict</code>)</li><li><code>json_schema</code>: строгая JSON схема (требуется <code>json_schema</code> параметр)</li></ul><p><strong>Tool Calling:</strong></p><ul><li><code>tools</code> — массив объектов (каждый: type, function.name, function.description, function.parameters по JSON Schema)</li><li>Модель решает, какие функции вызвать и с какими параметрами; вызовы возвращаются в <code>response_data.tool_calls</code></li><li><code>tool_calls</code> — массив объектов: id, type, function.name, function.arguments (JSON-строка); обработка и циклы — в сценарии</li><li><code>tool_choice</code>: строка &#39;none&#39;|&#39;auto&#39;|&#39;required&#39; или объект для принудительного вызова одной функции</li></ul><p><strong>Формат чанков (chunk_format):</strong></p><ul><li>Настройка отображения чанков из RAG через шаблоны с маркерами <code>$</code></li><li>Доступны: <code>$content</code> (обязательно) + любые поля из <code>chunk_metadata</code></li><li>Поддерживается fallback: <code>$field|fallback:значение</code> (если поле пустое/отсутствует)</li><li>Маркеры работают только с данными чанка (content + chunk_metadata)</li><li>Примеры: <code>&quot;[$username]: $content&quot;</code>, <code>&quot;[$category|fallback:Общее]: $content&quot;</code></li><li>Можно задать разные шаблоны для <code>chat_history</code>, <code>knowledge</code>, <code>other</code></li></ul></details><p><a id="embedding"></a></p><h3 id="embedding" tabindex="-1">embedding <a class="header-anchor" href="#embedding" aria-label="Permalink to &quot;embedding&quot;">​</a></h3><p><strong>Описание:</strong> Генерация embedding для текста через ИИ</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>text</code></strong> (<code>string</code>) — Текст для генерации embedding</li><li><strong><code>model</code></strong> (<code>string</code>, опционально) — Модель для генерации embedding (по умолчанию из настроек ai_client.default_embedding_model)</li><li><strong><code>dimensions</code></strong> (<code>integer</code>, опционально) — Размерность embedding (по умолчанию из настроек ai_client.default_embedding_dimensions). Поддерживается только для OpenAI text-embedding-3-small и text-embedding-3-large</li><li>🔑 <strong><code>ai_token</code></strong> (<code>string</code>) — AI API ключ из конфига тенанта (_config.ai_token)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки (например, ошибки валидации полей)</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>embedding</code></strong> (<code>array (of number)</code>) — Вектор embedding (список чисел)</li><li><strong><code>model</code></strong> (<code>string</code>) — Использованная модель</li><li><strong><code>dimensions</code></strong> (<code>integer</code>) — Размерность embedding</li><li><strong><code>total_tokens</code></strong> (<code>integer</code>) — Общее количество токенов</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔑 — поле, которое автоматически берется из конфигурации тенанта (_config) и не требует явной передачи в params</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;embedding&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # model: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # dimensions: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    ai_token</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span></code></pre></div><details><summary>📖 Дополнительная информация</summary><p><strong>Генерация векторных представлений текста (embeddings):</strong></p><ul><li>Используется для RAG (Retrieval-Augmented Generation) и векторного поиска</li><li>Возвращает массив чисел (<code>embedding</code>) - векторное представление текста</li></ul><p><strong>Размерность (dimensions):</strong></p><ul><li>По умолчанию 1024 (настраивается в <code>ai_client.default_embedding_dimensions</code>)</li><li>Поддерживается не всеми моделями (для моделей без поддержки размерность фиксирована)</li></ul><p><strong>Пример:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">embedding</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Текст для векторизации&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  dimensions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1024</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Ответ:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">success</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">response_data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  embedding</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.123</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">-0.456</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Вектор из 1024 чисел</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  dimensions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1024</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  total_tokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">15</span></span></code></pre></div></details><p><a id="download_service"></a></p><h2 id="download-service" tabindex="-1">download_service <a class="header-anchor" href="#download-service" aria-label="Permalink to &quot;download_service&quot;">​</a></h2><p><strong>Описание:</strong> Сервис для загрузки файлов по ссылкам и извлечения текстового содержимого</p><p><a id="download_and_extract"></a></p><h3 id="⭐-download-and-extract" tabindex="-1">⭐ download_and_extract <a class="header-anchor" href="#⭐-download-and-extract" aria-label="Permalink to &quot;⭐ download_and_extract&quot;">​</a></h3><p><strong>Описание:</strong> Загрузить файл по URL и извлечь текстовое содержимое. Поддержка форматов: PDF, DOCX, TXT, MD, HTML, CSV (в т.ч. Google Таблицы). Автоопределение типа по magic bytes, Content-Type или расширению</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта (обязательно, для изоляции файлов)</li><li><strong><code>url</code></strong> (<code>string</code>) — URL для загрузки файла (прямая ссылка, Google Drive, Google Docs/Sheets и т.д.)</li><li><strong><code>file_type</code></strong> (<code>string</code>, опционально, значения: [<code>pdf</code>, <code>docx</code>, <code>txt</code>, <code>md</code>, <code>html</code>, <code>csv</code>]) — Подсказка типа файла (pdf, docx, txt, md, html, csv). Если не указан - определяется автоматически по magic bytes, Content-Type или расширению</li><li><strong><code>keep_file</code></strong> (<code>boolean</code>, опционально) — Сохранить загруженный файл вместо автоудаления (по умолчанию false - файл удаляется после извлечения текста)</li><li><strong><code>max_file_size_mb</code></strong> (<code>integer</code>, опционально, диапазон: 1-500) — Максимальный размер файла в МБ для этого конкретного действия (переопределяет настройку по умолчанию). Проверяется через HEAD запрос перед загрузкой</li><li><strong><code>download_timeout_seconds</code></strong> (<code>integer</code>, опционально, диапазон: 10-3600) — Таймаут загрузки в секундах для этого конкретного действия (переопределяет настройку по умолчанию)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки: VALIDATION_ERROR (неверные параметры), FILE_TOO_LARGE (превышен размер), DOWNLOAD_FAILED (ошибка загрузки), UNSUPPORTED_FORMAT (неподдерживаемый формат), EXTRACTION_FAILED (ошибка извлечения текста), TIMEOUT (превышен таймаут), INTERNAL_ERROR (внутренняя ошибка)</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки (например, ошибки валидации полей)</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>file_text</code></strong> (<code>string</code>) — Извлеченный текст из файла</li><li><strong><code>file_path</code></strong> (<code>string</code>) (опционально) — Путь к загруженному файлу (только если keep_file=true, иначе null)</li><li><strong><code>file_metadata</code></strong> (<code>object</code>) — Метаданные файла</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;download_and_extract&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # file_type: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # keep_file: boolean (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # max_file_size_mb: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # download_timeout_seconds: integer (опционально)</span></span></code></pre></div><p><a id="invoice_service"></a></p><h2 id="invoice-service" tabindex="-1">invoice_service <a class="header-anchor" href="#invoice-service" aria-label="Permalink to &quot;invoice_service&quot;">​</a></h2><p><strong>Описание:</strong> Сервис для работы с инвойсами (создание, управление, обработка платежей)</p><p><a id="cancel_invoice"></a></p><h3 id="cancel-invoice" tabindex="-1">cancel_invoice <a class="header-anchor" href="#cancel-invoice" aria-label="Permalink to &quot;cancel_invoice&quot;">​</a></h3><p><strong>Описание:</strong> Отмена инвойса (установка флага is_cancelled)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>invoice_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID инвойса (обязательно)</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cancel_invoice&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    invoice_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span></code></pre></div><p><a id="confirm_payment"></a></p><h3 id="confirm-payment" tabindex="-1">confirm_payment <a class="header-anchor" href="#confirm-payment" aria-label="Permalink to &quot;confirm_payment&quot;">​</a></h3><p><strong>Описание:</strong> Подтверждение платежа (ответ на pre_checkout_query с подтверждением)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li><li><strong><code>bot_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID бота</li><li><strong><code>pre_checkout_query_id</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — ID запроса на подтверждение (обязательно)</li><li><strong><code>invoice_payload</code></strong> (<code>string</code>, опционально, мин. длина: 1) — ID инвойса из payload (опционально, для проверки статуса инвойса перед подтверждением)</li><li><strong><code>error_message</code></strong> (<code>string</code>, опционально) — Сообщение об ошибке при отклонении платежа (опционально, используется если инвойс отменен или уже оплачен)</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success (платеж подтвержден), failed (платеж отклонен по бизнес-логике), error (техническая ошибка)</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки. Возможные значения: INVOICE_CANCELLED (при result=failed - инвойс отменен), INVOICE_ALREADY_PAID (при result=failed - инвойс уже оплачен)</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;confirm_payment&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    bot_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    pre_checkout_query_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # invoice_payload: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # error_message: string (опционально)</span></span></code></pre></div><p><a id="create_invoice"></a></p><h3 id="create-invoice" tabindex="-1">create_invoice <a class="header-anchor" href="#create-invoice" aria-label="Permalink to &quot;create_invoice&quot;">​</a></h3><p><strong>Описание:</strong> Создание инвойса в БД и отправка/создание ссылки</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li><li><strong><code>bot_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID бота</li><li><strong><code>target_user_id</code></strong> (<code>integer</code>, опционально, мин: 1) — ID пользователя для создания инвойса (опционально, по умолчанию user_id из события)</li><li><strong><code>chat_id</code></strong> (<code>integer</code>, опционально) — ID чата для отправки (обязательно для отправки, не нужен для ссылки)</li><li><strong><code>title</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — Название товара/услуги (обязательно)</li><li><strong><code>description</code></strong> (<code>string</code>) — Описание товара/услуги</li><li><strong><code>amount</code></strong> (<code>integer</code>, обязательное, мин: 1) — Количество звезд (обязательно, целое число)</li><li><strong><code>currency</code></strong> (<code>string</code>, опционально) — Валюта (по умолчанию XTR для звезд)</li><li><strong><code>as_link</code></strong> (<code>boolean</code>, опционально) — Создать как ссылку вместо отправки (по умолчанию false - отправка)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>invoice_id</code></strong> (<code>integer</code>) — ID созданного инвойса</li><li><strong><code>invoice_message_id</code></strong> (<code>integer</code>) (опционально) — ID отправленного сообщения с инвойсом (если as_link=false)</li><li><strong><code>invoice_link</code></strong> (<code>string</code>) (опционально) — Ссылка на инвойс (если as_link=true)</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;create_invoice&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    bot_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # target_user_id: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # chat_id: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    title</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    description</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    amount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # currency: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # as_link: boolean (опционально)</span></span></code></pre></div><p><a id="get_invoice"></a></p><h3 id="get-invoice" tabindex="-1">get_invoice <a class="header-anchor" href="#get-invoice" aria-label="Permalink to &quot;get_invoice&quot;">​</a></h3><p><strong>Описание:</strong> Получение информации об инвойсе</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>invoice_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID инвойса (обязательно)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>invoice</code></strong> (<code>object</code>) — Данные инвойса</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get_invoice&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    invoice_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span></code></pre></div><p><a id="get_user_invoices"></a></p><h3 id="get-user-invoices" tabindex="-1">get_user_invoices <a class="header-anchor" href="#get-user-invoices" aria-label="Permalink to &quot;get_user_invoices&quot;">​</a></h3><p><strong>Описание:</strong> Получение всех инвойсов пользователя</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>target_user_id</code></strong> (<code>integer</code>, опционально, мин: 1) — ID пользователя для получения инвойсов (опционально, по умолчанию user_id из события)</li><li><strong><code>include_cancelled</code></strong> (<code>boolean</code>, опционально) — Включать отмененные инвойсы (по умолчанию false)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>invoices</code></strong> (<code>array (of object)</code>) — Массив инвойсов пользователя</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get_user_invoices&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # target_user_id: integer (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # include_cancelled: boolean (опционально)</span></span></code></pre></div><p><a id="mark_invoice_as_paid"></a></p><h3 id="mark-invoice-as-paid" tabindex="-1">mark_invoice_as_paid <a class="header-anchor" href="#mark-invoice-as-paid" aria-label="Permalink to &quot;mark_invoice_as_paid&quot;">​</a></h3><p><strong>Описание:</strong> Отметить инвойс как оплаченный (обработка события payment_successful)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>invoice_payload</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — ID инвойса из payload события (обязательно)</li><li><strong><code>telegram_payment_charge_id</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — ID платежа в Telegram (обязательно)</li><li><strong><code>paid_at</code></strong> (<code>string</code>, опционально) — Дата оплаты (опционально, по умолчанию текущая дата)</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mark_invoice_as_paid&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    invoice_payload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    telegram_payment_charge_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # paid_at: string (опционально)</span></span></code></pre></div><p><a id="reject_payment"></a></p><h3 id="reject-payment" tabindex="-1">reject_payment <a class="header-anchor" href="#reject-payment" aria-label="Permalink to &quot;reject_payment&quot;">​</a></h3><p><strong>Описание:</strong> Отклонение платежа (ответ на pre_checkout_query с отклонением)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li><li><strong><code>bot_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID бота</li><li><strong><code>pre_checkout_query_id</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — ID запроса на подтверждение (обязательно)</li><li><strong><code>error_message</code></strong> (<code>string</code>, опционально) — Сообщение об ошибке (опционально)</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;reject_payment&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    bot_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    pre_checkout_query_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # error_message: string (опционально)</span></span></code></pre></div><p><a id="scenario_helper"></a></p><h2 id="scenario-helper" tabindex="-1">scenario_helper <a class="header-anchor" href="#scenario-helper" aria-label="Permalink to &quot;scenario_helper&quot;">​</a></h2><p><strong>Описание:</strong> Вспомогательные утилиты для управления выполнением сценариев</p><p><a id="check_value_in_array"></a></p><h3 id="check-value-in-array" tabindex="-1">check_value_in_array <a class="header-anchor" href="#check-value-in-array" aria-label="Permalink to &quot;check_value_in_array&quot;">​</a></h3><p><strong>Описание:</strong> Проверка наличия значения в массиве</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>array</code></strong> (<code>array</code>) — Массив для проверки</li><li><strong><code>value</code></strong> (<code>any</code>) — Значение для поиска в массиве</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success (найдено), not_found (не найдено), error (ошибка)</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>response_index</code></strong> (<code>integer</code>) — Порядковый номер (индекс) первого вхождения значения в массиве (только при result: success)</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;check_value_in_array&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;value&quot;</span></span></code></pre></div><p><a id="choose_from_array"></a></p><h3 id="choose-from-array" tabindex="-1">choose_from_array <a class="header-anchor" href="#choose-from-array" aria-label="Permalink to &quot;choose_from_array&quot;">​</a></h3><p><strong>Описание:</strong> Выбор случайных элементов из массива без повторений</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>array</code></strong> (<code>array</code>) — Исходный массив для выбора элементов</li><li><strong><code>count</code></strong> (<code>integer</code>, обязательное, мин: 1) — Количество элементов для выбора (без повторений)</li><li><strong><code>seed</code></strong> (<code>any</code>, опционально) — Seed для детерминированной генерации (опционально, может быть числом, строкой или другим типом)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>random_list</code></strong> (<code>array</code>) — Массив выбранных элементов (без повторений)</li><li><strong><code>random_seed</code></strong> (<code>string</code>) (опционально) — Использованный seed (если был передан, сохраняется как есть)</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;choose_from_array&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # seed: any (опционально)</span></span></code></pre></div><p><a id="format_data_to_text"></a></p><h3 id="format-data-to-text" tabindex="-1">format_data_to_text <a class="header-anchor" href="#format-data-to-text" aria-label="Permalink to &quot;format_data_to_text&quot;">​</a></h3><p><strong>Описание:</strong> Форматирование структурированных данных (JSON/YAML) в текстовый формат для промптов и сообщений</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>format_type</code></strong> (<code>string</code>, обязательное, значения: [<code>list</code>, <code>structured</code>]) — Тип форматирования: &#39;list&#39; (простой список с шаблоном через $), &#39;structured&#39; (структурированный формат с заголовками и вложенными блоками)</li><li><strong><code>input_data</code></strong> (<code>any</code>) — Массив объектов для форматирования</li><li><strong><code>title</code></strong> (<code>string</code>, опционально) — Заголовок для списка (опционально)</li><li><strong><code>item_template</code></strong> (<code>string</code>, опционально, мин. длина: 1) — Шаблон элемента для формата &#39;list&#39; через $ (например: &#39;- &quot;$id&quot; - $description&#39;). Обязателен при format_type: &#39;list&#39;</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>formatted_text</code></strong> (<code>string</code>) — Отформатированный текст</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;format_data_to_text&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    format_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    input_data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;value&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # title: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # item_template: string (опционально)</span></span></code></pre></div><details><summary>📖 Дополнительная информация</summary><p><strong>Форматы:</strong></p><ul><li><strong><code>list</code></strong> — простой список с шаблоном через <code>$</code></li><li><strong><code>structured</code></strong> — структурированный формат: <code>name — description</code>, блок <code>Параметры:</code> с деталями</li></ul><p><strong>Примеры:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Формат list</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;format_data_to_text&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    format_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;list&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    title</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Доступные намерения:&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    item_template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;- &quot;$id&quot; - $description&#39;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    input_data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;{storage_values.ai_router.intents}&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Результат в _cache.format_data_to_text.formatted_text:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Доступные намерения:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># - &quot;random_user&quot; - Выбрать случайного пользователя</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Формат structured</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;format_data_to_text&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    format_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;structured&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    title</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Доступные действия:&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    input_data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;{storage_values.ai_router.actions}&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Результат в _cache.format_data_to_text.formatted_text:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Доступные действия:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># call_random_user — Выбрать N пользователей из группы</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   Параметры:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   - count (integer) — Количество пользователей. По умолчанию: 1</span></span></code></pre></div><p><strong>Важно:</strong></p><ul><li>Шаблон для <code>list</code> использует <code>$</code> вместо <code>{}</code> для избежания конфликта с плейсхолдерами</li><li>Результат сохраняется в <code>_cache.format_data_to_text.formatted_text</code> (по умолчанию)</li><li>Если указан <code>_namespace</code> в params, результат будет в <code>_cache.{_namespace}.formatted_text</code></li></ul></details><p><a id="generate_array"></a></p><h3 id="generate-array" tabindex="-1">generate_array <a class="header-anchor" href="#generate-array" aria-label="Permalink to &quot;generate_array&quot;">​</a></h3><p><strong>Описание:</strong> Генерация массива случайных чисел в заданном диапазоне (по умолчанию без повторений)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>min</code></strong> (<code>integer</code>) — Минимальное значение (включительно)</li><li><strong><code>max</code></strong> (<code>integer</code>) — Максимальное значение (включительно)</li><li><strong><code>count</code></strong> (<code>integer</code>, обязательное, мин: 1) — Количество чисел для генерации</li><li><strong><code>seed</code></strong> (<code>any</code>, опционально) — Seed для детерминированной генерации (опционально, может быть числом, строкой или другим типом)</li><li><strong><code>allow_duplicates</code></strong> (<code>boolean</code>, опционально) — Разрешить повторения в массиве (по умолчанию false - без повторений)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>random_list</code></strong> (<code>array (of integer)</code>) — Массив сгенерированных чисел</li><li><strong><code>random_seed</code></strong> (<code>string</code>) (опционально) — Использованный seed (если был передан, сохраняется как есть)</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;generate_array&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    min</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    max</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # seed: any (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # allow_duplicates: boolean (опционально)</span></span></code></pre></div><p><a id="generate_int"></a></p><h3 id="generate-int" tabindex="-1">generate_int <a class="header-anchor" href="#generate-int" aria-label="Permalink to &quot;generate_int&quot;">​</a></h3><p><strong>Описание:</strong> Генерация случайного целого числа в заданном диапазоне</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>min</code></strong> (<code>integer</code>) — Минимальное значение (включительно)</li><li><strong><code>max</code></strong> (<code>integer</code>) — Максимальное значение (включительно)</li><li><strong><code>seed</code></strong> (<code>any</code>, опционально) — Seed для детерминированной генерации (опционально, может быть числом, строкой или другим типом)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>random_value</code></strong> (<code>integer</code>) — Сгенерированное случайное число</li><li><strong><code>random_seed</code></strong> (<code>string</code>) (опционально) — Использованный seed (если был передан, сохраняется как есть)</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;generate_int&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    min</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    max</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # seed: any (опционально)</span></span></code></pre></div><p><a id="generate_unique_id"></a></p><h3 id="generate-unique-id" tabindex="-1">generate_unique_id <a class="header-anchor" href="#generate-unique-id" aria-label="Permalink to &quot;generate_unique_id&quot;">​</a></h3><p><strong>Описание:</strong> Генерация уникального ID через автоинкремент в БД (детерминированная генерация - при одинаковых seed возвращает тот же ID). Если seed не указан, генерируется случайный UUID</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>seed</code></strong> (<code>string</code>, опционально, мин. длина: 1) — Seed для генерации ID. При повторном запросе с тем же seed вернется тот же ID. Если не указан, генерируется случайный UUID</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>unique_id</code></strong> (<code>integer</code>) — Уникальный ID (гарантированно уникальный, при одинаковых входных данных возвращается тот же ID)</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;generate_unique_id&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # seed: string (опционально)</span></span></code></pre></div><p><a id="modify_array"></a></p><h3 id="modify-array" tabindex="-1">modify_array <a class="header-anchor" href="#modify-array" aria-label="Permalink to &quot;modify_array&quot;">​</a></h3><p><strong>Описание:</strong> Модификация массива: добавление, удаление элементов или очистка</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>operation</code></strong> (<code>string</code>, обязательное, значения: [<code>add</code>, <code>remove</code>, <code>clear</code>]) — Операция: &#39;add&#39; (добавить элемент), &#39;remove&#39; (удалить элемент), &#39;clear&#39; (очистить массив)</li><li><strong><code>array</code></strong> (<code>array</code>) — Исходный массив для модификации</li><li><strong><code>value</code></strong> (<code>any</code>, опционально) — Значение для добавления или удаления (обязательно для операций &#39;add&#39; и &#39;remove&#39;)</li><li><strong><code>skip_duplicates</code></strong> (<code>boolean</code>, опционально) — Пропускать дубликаты при добавлении (по умолчанию true - не добавлять если элемент уже есть)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success (успешно), not_found (элемент не найден при операции remove), error (ошибка)</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>modified_array</code></strong> (<code>array</code>) — Модифицированный массив</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;modify_array&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    operation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # value: any (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # skip_duplicates: boolean (опционально)</span></span></code></pre></div><p><a id="set_cache"></a></p><h3 id="set-cache" tabindex="-1">set_cache <a class="header-anchor" href="#set-cache" aria-label="Permalink to &quot;set_cache&quot;">​</a></h3><p><strong>Описание:</strong> Установка временных данных в кэш сценария. Все переданные параметры возвращаются в response_data и автоматически попадают в плоский _cache по умолчанию.</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>cache</code></strong> (<code>object</code>) — Объект с данными для установки в кэш. Все переданные параметры будут доступны в последующих шагах сценария через {_cache.ключ} (плоский доступ по умолчанию)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Все переданные параметры из объекта cache возвращаются в response_data и автоматически попадают в плоский _cache по умолчанию. <ul><li><strong><code>*</code></strong> (<code>any</code>) — Динамические ключи из переданного объекта cache. Все ключи доступны через {_cache.имя_ключа} (плоский доступ по умолчанию)</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;set_cache&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    cache</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {}</span></span></code></pre></div><details><summary>📖 Дополнительная информация</summary><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;set_cache&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      cache</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        selected_user</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@username&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        reason</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Случайный выбор&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        metadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          timestamp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2024-01-15&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          source</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ai_router&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;send_message&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        Пользователь: {_cache.selected_user}</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        Причина: {_cache.reason}</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        Время: {_cache.metadata.timestamp}</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        Источник: {_cache.metadata.source}</span></span></code></pre></div><p><strong>Важно:</strong></p><ul><li>Данные должны быть переданы в ключе <code>cache</code> в params</li><li>Это позволяет явно указать, что именно нужно кэшировать</li><li>Весь контекст сценария (user_id, chat_id, bot_id и др.) не попадет в кэш</li></ul><p><strong>Доступ к данным:</strong></p><ul><li>Все переданные параметры доступны через плейсхолдеры <code>{_cache.ключ}</code> (плоский доступ по умолчанию)</li><li>Поддерживаются вложенные структуры: <code>{_cache.metadata.timestamp}</code></li><li>Данные автоматически очищаются после завершения выполнения сценария</li></ul></details><p><a id="sleep"></a></p><h3 id="sleep" tabindex="-1">sleep <a class="header-anchor" href="#sleep" aria-label="Permalink to &quot;sleep&quot;">​</a></h3><p><strong>Описание:</strong> Задержка выполнения на указанное количество секунд</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>seconds</code></strong> (<code>float</code>, обязательное, мин: 0.0) — Количество секунд задержки (можно использовать дробные значения, например 0.5 или 22.5)</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sleep&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;value&quot;</span></span></code></pre></div><p><a id="scenario_processor"></a></p><h2 id="scenario-processor" tabindex="-1">scenario_processor <a class="header-anchor" href="#scenario-processor" aria-label="Permalink to &quot;scenario_processor&quot;">​</a></h2><p><strong>Описание:</strong> Сервис для обработки событий по сценариям</p><p><a id="execute_scenario"></a></p><h3 id="execute-scenario" tabindex="-1">execute_scenario <a class="header-anchor" href="#execute-scenario" aria-label="Permalink to &quot;execute_scenario&quot;">​</a></h3><p><strong>Описание:</strong> Выполнение сценария или массива сценариев по имени</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>scenario</code></strong> (<code>string|array</code>) — Название сценария (строка) или массив названий сценариев</li><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID tenant&#39;а (передается автоматически из контекста)</li><li><strong><code>return_cache</code></strong> (<code>boolean</code>, опционально) — Возвращать ли _cache из выполненного сценария (по умолчанию true). ВАЖНО: работает только для одиночных сценариев (строка), для массива сценариев игнорируется и кэш не возвращается</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа с результатом выполнения сценария <ul><li><strong><code>scenario_result</code></strong> (<code>string</code>) — Результат выполнения сценария: success, error, abort, break, stop</li><li><strong><code>_cache</code></strong> (<code>object</code>) (опционально) — Кэш из выполненного сценария, возвращается только для одиночных сценариев (не для массива), если return_cache=true и в сценарии был установлен _cache через set_cache. Данные попадают в _cache[action_name] автоматически</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;execute_scenario&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    scenario</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;value&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # return_cache: boolean (опционально)</span></span></code></pre></div><p><a id="wait_for_action"></a></p><h3 id="wait-for-action" tabindex="-1">wait_for_action <a class="header-anchor" href="#wait-for-action" aria-label="Permalink to &quot;wait_for_action&quot;">​</a></h3><p><strong>Описание:</strong> Ожидание завершения асинхронного действия по action_id. Возвращает результат основного действия AS IS (как будто оно выполнилось напрямую)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>action_id</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — Уникальный ID асинхронного действия для ожидания</li><li><strong><code>timeout</code></strong> (<code>integer</code>, опционально, мин: 0.0) — Таймаут ожидания в секундах (опционально). При истечении таймаута wait_for_action возвращает ошибку timeout, но выполнение сценария продолжается. Основное асинхронное действие продолжает выполняться в фоне даже после таймаута.</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат зависит от основного действия: если основное действие завершилось успешно - возвращается его результат (success/error и т.д.), если ошибка ожидания (таймаут, Future не найден) - возвращается ошибка wait_for_action (timeout/error)</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа. Если основное действие завершилось успешно - возвращаются данные основного действия (полностью подменяются), если ошибка ожидания - отсутствует</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;wait_for_action&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    action_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # timeout: integer (опционально)</span></span></code></pre></div><p><a id="storage_hub"></a></p><h2 id="storage-hub" tabindex="-1">storage_hub <a class="header-anchor" href="#storage-hub" aria-label="Permalink to &quot;storage_hub&quot;">​</a></h2><p><strong>Описание:</strong> Сервис для управления tenant storage</p><p><a id="delete_storage"></a></p><h3 id="delete-storage" tabindex="-1">delete_storage <a class="header-anchor" href="#delete-storage" aria-label="Permalink to &quot;delete_storage&quot;">​</a></h3><p><strong>Описание:</strong> Удаление значений или групп из storage. Если указан key или key_pattern - удаляется значение, иначе удаляется группа</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта (обязательно)</li><li><strong><code>group_key</code></strong> (<code>string</code>, опционально) — Ключ группы (точное совпадение, приоритет над group_key_pattern)</li><li><strong><code>group_key_pattern</code></strong> (<code>string</code>, опционально, мин. длина: 1) — Паттерн для поиска группы (ILIKE, используется если group_key не указан)</li><li><strong><code>key</code></strong> (<code>string</code>, опционально) — Ключ атрибута (точное совпадение, приоритет над key_pattern). Если указан - удаляется значение, иначе удаляется группа</li><li><strong><code>key_pattern</code></strong> (<code>string</code>, опционально, мин. длина: 1) — Паттерн для поиска ключа (ILIKE, используется если key не указан). Если указан - удаляется значение, иначе удаляется группа</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error, not_found</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;delete_storage&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # group_key: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # group_key_pattern: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key_pattern: string (опционально)</span></span></code></pre></div><p><a id="get_storage"></a></p><h3 id="get-storage" tabindex="-1">get_storage <a class="header-anchor" href="#get-storage" aria-label="Permalink to &quot;get_storage&quot;">​</a></h3><p><strong>Описание:</strong> Получение значений storage для тенанта. Поддерживает получение всех значений, группы, конкретного значения, а также поиск по паттернам</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта (обязательно)</li><li><strong><code>group_key</code></strong> (<code>string</code>, опционально) — Ключ группы (точное совпадение, приоритет над group_key_pattern)</li><li><strong><code>group_key_pattern</code></strong> (<code>string</code>, опционально, мин. длина: 1) — Паттерн для поиска группы (ILIKE, используется если group_key не указан)</li><li><strong><code>key</code></strong> (<code>string</code>, опционально) — Ключ атрибута (точное совпадение, приоритет над key_pattern). Используется только если указан group_key или group_key_pattern</li><li><strong><code>key_pattern</code></strong> (<code>string</code>, опционально, мин. длина: 1) — Паттерн для поиска ключа (ILIKE, используется если key не указан). Используется только если указан group_key или group_key_pattern</li><li><strong><code>format</code></strong> (<code>boolean</code>, опционально) — Если true, возвращает дополнительное поле formatted_text с данными в формате YAML</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error, not_found</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>storage_values</code></strong> (<code>any</code>) — Запрошенные данные. Если указаны group_key и key - возвращается прямое значение (примитив или объект как есть). Если указан только group_key - возвращается структура группы {key: value}. Если ничего не указано - возвращается полная структура {group_key: {key: value}}</li><li><strong><code>formatted_text</code></strong> (<code>string</code>) (опционально) — Отформатированный текст в формате YAML (возвращается только если format=true)</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get_storage&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # group_key: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # group_key_pattern: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key_pattern: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # format: boolean (опционально)</span></span></code></pre></div><p><a id="get_storage_groups"></a></p><h3 id="get-storage-groups" tabindex="-1">get_storage_groups <a class="header-anchor" href="#get-storage-groups" aria-label="Permalink to &quot;get_storage_groups&quot;">​</a></h3><p><strong>Описание:</strong> Получение списка уникальных ключей групп для тенанта. Возвращает только список group_key без значений (с ограничением на количество групп)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта (обязательно)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>group_keys</code></strong> (<code>array (of string)</code>) — Список уникальных ключей групп (ограничен настройкой storage_groups_max_limit)</li><li><strong><code>is_truncated</code></strong> (<code>boolean</code>) (опционально) — Флаг, указывающий что список был обрезан (true если групп больше лимита, иначе поле отсутствует)</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get_storage_groups&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span></code></pre></div><p><a id="set_storage"></a></p><h3 id="set-storage" tabindex="-1">set_storage <a class="header-anchor" href="#set-storage" aria-label="Permalink to &quot;set_storage&quot;">​</a></h3><p><strong>Описание:</strong> Установка значений storage для тенанта. Поддерживает смешанный подход: полная структура через values или частичная через group_key/key/value</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта (обязательно)</li><li><strong><code>group_key</code></strong> (<code>string</code>, опционально) — Ключ группы (с поддержкой плейсхолдеров). Если указан, используется смешанный подход</li><li><strong><code>key</code></strong> (<code>string</code>, опционально) — Ключ значения (с поддержкой плейсхолдеров). Используется вместе с group_key</li><li><strong><code>value</code></strong> (<code>any</code>, опционально) — Значение для установки. Используется вместе с group_key и key</li><li><strong><code>values</code></strong> (<code>object</code>, опционально) — Структура для установки. Если указан group_key - структура для группы {key: value}, иначе полная структура {group_key: {key: value}}</li><li><strong><code>format</code></strong> (<code>boolean</code>, опционально) — Если true, возвращает дополнительное поле formatted_text с данными в формате YAML</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>storage_values</code></strong> (<code>any</code>) — Установленные данные. Если установлено одно значение (group_key + key + value) - возвращается прямое значение (примитив или объект как есть). Если установлена группа (group_key + values) - возвращается структура группы {key: value}. Если установлена полная структура (values) - возвращается полная структура {group_key: {key: value}}</li><li><strong><code>formatted_text</code></strong> (<code>string</code>) (опционально) — Отформатированный текст в формате YAML (возвращается только если format=true)</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;set_storage&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # group_key: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # value: any (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # values: object (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # format: boolean (опционально)</span></span></code></pre></div><p><a id="telegram_bot_api"></a></p><h2 id="telegram-bot-api" tabindex="-1">telegram_bot_api <a class="header-anchor" href="#telegram-bot-api" aria-label="Permalink to &quot;telegram_bot_api&quot;">​</a></h2><p><strong>Описание:</strong> Сервис для выполнения действий через Telegram Bot API</p><p><a id="answer_callback_query"></a></p><h3 id="answer-callback-query" tabindex="-1">answer_callback_query <a class="header-anchor" href="#answer-callback-query" aria-label="Permalink to &quot;answer_callback_query&quot;">​</a></h3><p><strong>Описание:</strong> Ответ на callback query (всплывающее уведомление)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>bot_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID бота</li><li><strong><code>callback_query_id</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — ID callback query от Telegram</li><li><strong><code>text</code></strong> (<code>string</code>, опционально, макс. длина: 200) — Текст уведомления</li><li><strong><code>show_alert</code></strong> (<code>boolean</code>, опционально) — Показать как alert вместо уведомления</li><li><strong><code>cache_time</code></strong> (<code>integer</code>, опционально, диапазон: 0-3600) — Время кэширования ответа (секунды)</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;answer_callback_query&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    bot_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    callback_query_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # text: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # show_alert: boolean (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # cache_time: integer (опционально)</span></span></code></pre></div><p><a id="build_keyboard"></a></p><h3 id="build-keyboard" tabindex="-1">build_keyboard <a class="header-anchor" href="#build-keyboard" aria-label="Permalink to &quot;build_keyboard&quot;">​</a></h3><p><strong>Описание:</strong> Построение клавиатуры из массива ID с использованием шаблонов</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>items</code></strong> (<code>array</code>) — Массив ID для генерации кнопок</li><li><strong><code>keyboard_type</code></strong> (<code>string</code>, обязательное, значения: [<code>inline</code>, <code>reply</code>]) — Тип клавиатуры (inline или reply)</li><li><strong><code>text_template</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — Шаблон текста с плейсхолдером $value$</li><li><strong><code>callback_template</code></strong> (<code>string</code>, опционально, мин. длина: 1) — Шаблон callback_data для inline (обязателен для inline)</li><li><strong><code>buttons_per_row</code></strong> (<code>integer</code>, опционально, диапазон: 1-8) — Количество кнопок в строке</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат построения</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные клавиатуры <ul><li><strong><code>keyboard</code></strong> (<code>array</code>) — Массив рядов кнопок</li><li><strong><code>keyboard_type</code></strong> (<code>string</code>) — Тип клавиатуры</li><li><strong><code>rows_count</code></strong> (<code>integer</code>) — Количество рядов</li><li><strong><code>buttons_count</code></strong> (<code>integer</code>) — Количество кнопок</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;build_keyboard&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    items</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    keyboard_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    text_template</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # callback_template: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # buttons_per_row: integer (опционально)</span></span></code></pre></div><p><a id="delete_message"></a></p><h3 id="delete-message" tabindex="-1">delete_message <a class="header-anchor" href="#delete-message" aria-label="Permalink to &quot;delete_message&quot;">​</a></h3><p><strong>Описание:</strong> Удаление сообщения ботом</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>bot_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID бота</li><li><strong><code>delete_message_id</code></strong> (<code>integer</code>, опционально, мин: 1) — ID сообщения для удаления</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат удаления</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;delete_message&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    bot_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # delete_message_id: integer (опционально)</span></span></code></pre></div><p><a id="restrict_chat_member"></a></p><h3 id="restrict-chat-member" tabindex="-1">restrict_chat_member <a class="header-anchor" href="#restrict-chat-member" aria-label="Permalink to &quot;restrict_chat_member&quot;">​</a></h3><p><strong>Описание:</strong> Ограничение участника в супергруппе (права по группам: сообщения, вложения, другое, управление)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>bot_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID бота</li><li><strong><code>chat_id</code></strong> (<code>integer|string</code>) — ID чата (супергруппы)</li><li><strong><code>target_user_id</code></strong> (<code>integer</code>) — ID пользователя для ограничения (целевой; не конфликтует с user_id из контекста)</li><li><strong><code>messages</code></strong> (<code>boolean</code>, опционально) — Разрешить текстовые сообщения, контакты, локации и т.д. Если не указано — не передаётся</li><li><strong><code>attachments</code></strong> (<code>boolean</code>, опционально) — Разрешить аудио, документы, фото, видео, видео-заметки, голос. Если не указано — не передаётся</li><li><strong><code>other</code></strong> (<code>boolean</code>, опционально) — Разрешить опросы, стикеры, игры, инлайн, превью ссылок. Если не указано — не передаётся</li><li><strong><code>management</code></strong> (<code>boolean</code>, опционально) — Разрешить менять инфо чата, приглашать, пинить, управлять топиками. Если не указано — не передаётся</li><li><strong><code>until_date</code></strong> (<code>integer</code>, опционально, мин: 0) — Unix-время снятия ограничения; 0 или не указано — навсегда</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;restrict_chat_member&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    bot_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    chat_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;value&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    target_user_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # messages: boolean (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # attachments: boolean (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # other: boolean (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # management: boolean (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # until_date: integer (опционально)</span></span></code></pre></div><p><a id="send_message"></a></p><h3 id="send-message" tabindex="-1">send_message <a class="header-anchor" href="#send-message" aria-label="Permalink to &quot;send_message&quot;">​</a></h3><p><strong>Описание:</strong> Отправка сообщения ботом</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>bot_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID бота</li><li><strong><code>target_chat_id</code></strong> (<code>integer|array|string</code>, опционально) — ID чата или массив ID чатов</li><li><strong><code>text</code></strong> (<code>string</code>, опционально) — Текст сообщения</li><li><strong><code>parse_mode</code></strong> (<code>string</code>, опционально, значения: [<code>HTML</code>, <code>Markdown</code>, <code>MarkdownV2</code>]) — Режим разбора (HTML, Markdown, MarkdownV2)</li><li><strong><code>message_edit</code></strong> (<code>integer|boolean|string</code>, опционально) — ID сообщения для редактирования или флаг</li><li><strong><code>message_reply</code></strong> (<code>integer|boolean|string</code>, опционально) — Ответ на сообщение: true (ответ на текущее из события), integer/string (ID сообщения)</li><li><strong><code>inline</code></strong> (<code>array</code>, опционально) — Inline-клавиатура</li><li><strong><code>reply</code></strong> (<code>array</code>, опционально) — Reply-клавиатура</li><li><strong><code>attachment</code></strong> (<code>array</code>, опционально) — Вложения</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат отправки</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>last_message_id</code></strong> (<code>integer</code>) — ID отправленного сообщения</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;send_message&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    bot_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # target_chat_id: integer|array|string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # text: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # parse_mode: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # message_edit: integer|boolean|string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # message_reply: integer|boolean|string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # inline: array (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # reply: array (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # attachment: array (опционально)</span></span></code></pre></div><p><a id="user_hub"></a></p><h2 id="user-hub" tabindex="-1">user_hub <a class="header-anchor" href="#user-hub" aria-label="Permalink to &quot;user_hub&quot;">​</a></h2><p><strong>Описание:</strong> Центральный сервис для управления состояниями пользователей</p><p><a id="clear_user_state"></a></p><h3 id="clear-user-state" tabindex="-1">clear_user_state <a class="header-anchor" href="#clear-user-state" aria-label="Permalink to &quot;clear_user_state&quot;">​</a></h3><p><strong>Описание:</strong> Очистка состояния пользователя</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>user_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID пользователя Telegram</li><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;clear_user_state&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    user_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span></code></pre></div><p><a id="delete_user_storage"></a></p><h3 id="delete-user-storage" tabindex="-1">delete_user_storage <a class="header-anchor" href="#delete-user-storage" aria-label="Permalink to &quot;delete_user_storage&quot;">​</a></h3><p><strong>Описание:</strong> Удаление значений из storage пользователя</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li><li><strong><code>user_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID пользователя Telegram</li><li><strong><code>key</code></strong> (<code>string</code>, опционально) — Ключ для удаления конкретного значения (точное совпадение, приоритет над key_pattern). Если не указаны key и key_pattern - удаляются все записи пользователя</li><li><strong><code>key_pattern</code></strong> (<code>string</code>, опционально, мин. длина: 1) — Паттерн для поиска ключей для удаления (ILIKE, используется если key не указан). Если не указаны key и key_pattern - удаляются все записи пользователя</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error, not_found</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки (например, ошибки валидации полей)</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;delete_user_storage&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    user_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key_pattern: string (опционально)</span></span></code></pre></div><p><a id="get_tenant_users"></a></p><h3 id="get-tenant-users" tabindex="-1">get_tenant_users <a class="header-anchor" href="#get-tenant-users" aria-label="Permalink to &quot;get_tenant_users&quot;">​</a></h3><p><strong>Описание:</strong> Получение списка всех user_id для указанного тенанта</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>) — ID тенанта</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>user_ids</code></strong> (<code>array (of integer)</code>) — Массив ID пользователей Telegram</li><li><strong><code>user_count</code></strong> (<code>integer</code>) — Количество пользователей</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get_tenant_users&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span></code></pre></div><p><a id="get_user_state"></a></p><h3 id="get-user-state" tabindex="-1">get_user_state <a class="header-anchor" href="#get-user-state" aria-label="Permalink to &quot;get_user_state&quot;">​</a></h3><p><strong>Описание:</strong> Получение состояния пользователя с проверкой истечения</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>user_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID пользователя Telegram</li><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li>🔀 <strong><code>user_state</code></strong> (<code>string</code>) (опционально) — Состояние пользователя или None если истекло/не установлено</li><li><strong><code>user_state_expired_at</code></strong> (<code>string</code>) (опционально) — Время истечения состояния (ISO) или None если не установлено</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get_user_state&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    user_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span></code></pre></div><p><a id="get_user_storage"></a></p><h3 id="get-user-storage" tabindex="-1">get_user_storage <a class="header-anchor" href="#get-user-storage" aria-label="Permalink to &quot;get_user_storage&quot;">​</a></h3><p><strong>Описание:</strong> Получение значений storage для пользователя. Поддерживает получение всех значений, конкретного значения (key) или поиск по паттерну (key_pattern)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li><li><strong><code>user_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID пользователя Telegram</li><li><strong><code>key</code></strong> (<code>string</code>, опционально) — Ключ для получения конкретного значения (точное совпадение, приоритет над key_pattern)</li><li><strong><code>key_pattern</code></strong> (<code>string</code>, опционально, мин. длина: 1) — Паттерн для поиска ключей (ILIKE, используется если key не указан)</li><li><strong><code>format</code></strong> (<code>boolean</code>, опционально) — Если true, возвращает дополнительное поле formatted_text с данными в формате YAML</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p>`,448),s("ul",null,[s("li",null,[s("strong",null,[s("code",null,"result")]),e(" ("),s("code",null,"string"),e(") — Результат: success, error, not_found")]),s("li",null,[s("strong",null,[s("code",null,"error")]),e(" ("),s("code",null,"object"),e(") (опционально) — Структура ошибки "),s("ul",null,[s("li",null,[s("strong",null,[s("code",null,"code")]),e(" ("),s("code",null,"string"),e(") — Код ошибки")]),s("li",null,[s("strong",null,[s("code",null,"message")]),e(" ("),s("code",null,"string"),e(") — Сообщение об ошибке")]),s("li",null,[s("strong",null,[s("code",null,"details")]),e(" ("),s("code",null,"array"),e(") (опционально) — Детали ошибки")])])]),s("li",null,[s("strong",null,[s("code",null,"response_data")]),e(" ("),s("code",null,"object"),e(") — "),s("ul",null,[s("li",{"key:":"",value:""},[e("🔀 "),s("strong",null,[s("code",null,"user_storage_values")]),e(" ("),s("code",null,"any"),e(") — Запрошенные данные. Если указан key - возвращается прямое значение (примитив или объект как есть). Если ничего не указано - возвращается полная структура")]),s("li",null,[s("strong",null,[s("code",null,"formatted_text")]),e(" ("),s("code",null,"string"),e(") (опционально) — Отформатированный текст в формате YAML (возвращается только если format=true)")])])])],-1),i(`<p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get_user_storage&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    user_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key_pattern: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # format: boolean (опционально)</span></span></code></pre></div><p><a id="get_users_by_storage_value"></a></p><h3 id="get-users-by-storage-value" tabindex="-1">get_users_by_storage_value <a class="header-anchor" href="#get-users-by-storage-value" aria-label="Permalink to &quot;get_users_by_storage_value&quot;">​</a></h3><p><strong>Описание:</strong> Поиск пользователей по ключу и значению в storage. Позволяет найти всех пользователей, у которых в storage есть определенный ключ с определенным значением (например, найти всех пользователей с подключенной подпиской)</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li><li><strong><code>key</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — Ключ для поиска в storage</li><li><strong><code>value</code></strong> (<code>string|integer|float|boolean|array|object</code>) — Значение для поиска (может быть простым типом или JSON объектом)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — <ul><li>🔀 <strong><code>user_ids</code></strong> (<code>array (of integer)</code>) — Массив ID пользователей Telegram, у которых storage[key] == value</li><li><strong><code>user_count</code></strong> (<code>integer</code>) — Количество найденных пользователей</li></ul></li></ul><p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get_users_by_storage_value&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;value&quot;</span></span></code></pre></div><p><a id="set_user_state"></a></p><h3 id="set-user-state" tabindex="-1">set_user_state <a class="header-anchor" href="#set-user-state" aria-label="Permalink to &quot;set_user_state&quot;">​</a></h3><p><strong>Описание:</strong> Установка состояния пользователя</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>user_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID пользователя Telegram</li><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li><li><strong><code>state</code></strong> (<code>string</code>, опционально) — Состояние пользователя (None или пустая строка для сброса)</li><li><strong><code>expires_in_seconds</code></strong> (<code>integer</code>, опционально, мин: 0) — Время истечения в секундах (None или 0 = навсегда, устанавливается 3000 год)</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</li></ul></details><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li><li><strong><code>response_data</code></strong> (<code>object</code>) — Данные ответа <ul><li><strong><code>user_state</code></strong> (<code>string</code>) (опционально) — Состояние пользователя или None если истекло/не установлено</li><li><strong><code>user_state_expired_at</code></strong> (<code>string</code>) (опционально) — Время истечения состояния (ISO) или None если не установлено</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;set_user_state&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    user_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # state: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # expires_in_seconds: integer (опционально)</span></span></code></pre></div><p><a id="set_user_storage"></a></p><h3 id="set-user-storage" tabindex="-1">set_user_storage <a class="header-anchor" href="#set-user-storage" aria-label="Permalink to &quot;set_user_storage&quot;">​</a></h3><p><strong>Описание:</strong> Установка значений storage для пользователя. Поддерживает смешанный подход: полная структура через values или частичная через key/value</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>tenant_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID тенанта</li><li><strong><code>user_id</code></strong> (<code>integer</code>, обязательное, мин: 1) — ID пользователя Telegram</li><li><strong><code>key</code></strong> (<code>string</code>, опционально) — Ключ значения (с поддержкой плейсхолдеров). Если указан, используется смешанный подход</li><li><strong><code>value</code></strong> (<code>any</code>, опционально) — Значение для установки. Используется вместе с key</li><li><strong><code>values</code></strong> (<code>object</code>, опционально) — Полная структура для установки {key: value}. Используется только если не указан key</li><li><strong><code>format</code></strong> (<code>boolean</code>, опционально) — Если true, возвращает дополнительное поле formatted_text с данными в формате YAML</li></ul><details><summary>⚙️ Дополнительные параметры</summary><ul><li><p><strong><code>_namespace</code></strong> (<code>string</code>) (опционально) — Кастомный ключ для создания вложенности в <code>_cache</code>. Если указан, данные сохраняются в <code>_cache[_namespace]</code> вместо плоского кэша. Используется для контроля перезаписи при повторных вызовах одного действия. Доступ через <code>{_cache._namespace.field}</code>. По умолчанию данные мержатся напрямую в <code>_cache</code> (плоское кэширование).</p></li><li><p><strong><code>_response_key</code></strong> (<code>string</code>) (опционально) — Кастомное имя для основного поля результата (помечено 🔀). Если указано, основное поле будет сохранено в <code>_cache</code> под указанным именем вместо стандартного. Доступ через <code>{_cache.{_response_key}}</code>. Работает только для действий, поддерживающих переименование основного поля.</p></li></ul></details><p><strong>Выходные параметры:</strong></p>`,33),s("ul",null,[s("li",null,[s("strong",null,[s("code",null,"result")]),e(" ("),s("code",null,"string"),e(") — Результат: success, error")]),s("li",null,[s("strong",null,[s("code",null,"error")]),e(" ("),s("code",null,"object"),e(") (опционально) — Структура ошибки "),s("ul",null,[s("li",null,[s("strong",null,[s("code",null,"code")]),e(" ("),s("code",null,"string"),e(") — Код ошибки")]),s("li",null,[s("strong",null,[s("code",null,"message")]),e(" ("),s("code",null,"string"),e(") — Сообщение об ошибке")]),s("li",null,[s("strong",null,[s("code",null,"details")]),e(" ("),s("code",null,"array"),e(") (опционально) — Детали ошибки")])])]),s("li",null,[s("strong",null,[s("code",null,"response_data")]),e(" ("),s("code",null,"object"),e(") — "),s("ul",null,[s("li",{"key:":"",value:""},[e("🔀 "),s("strong",null,[s("code",null,"user_storage_values")]),e(" ("),s("code",null,"any"),e(") — Установленные данные. Если установлено одно значение (key + value) - возвращается прямое значение (примитив или объект как есть). Если установлена структура (values без key) - возвращается структура")]),s("li",null,[s("strong",null,[s("code",null,"formatted_text")]),e(" ("),s("code",null,"string"),e(") (опционально) — Отформатированный текст в формате YAML (возвращается только если format=true)")])])])],-1),i(`<p><strong>Примечание:</strong></p><ul><li>🔀 — поле, которое можно переименовать через параметр <code>_response_key</code> для удобного доступа к данным</li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;set_user_storage&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tenant_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    user_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # key: string (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # value: any (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # values: object (опционально)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # format: boolean (опционально)</span></span></code></pre></div><p><a id="validator"></a></p><h2 id="validator" tabindex="-1">validator <a class="header-anchor" href="#validator" aria-label="Permalink to &quot;validator&quot;">​</a></h2><p><strong>Описание:</strong> Сервис для валидации условий в сценариях</p><p><a id="validate"></a></p><h3 id="validate" tabindex="-1">validate <a class="header-anchor" href="#validate" aria-label="Permalink to &quot;validate&quot;">​</a></h3><p><strong>Описание:</strong> Валидация условия с возвратом результата</p><p><strong>Входные параметры:</strong></p><ul><li><strong><code>condition</code></strong> (<code>string</code>, обязательное, мин. длина: 1) — Условие для валидации (поддерживает все операторы condition_parser)</li></ul><p><strong>Выходные параметры:</strong></p><ul><li><strong><code>result</code></strong> (<code>string</code>) — Результат: success, failed, error</li><li><strong><code>error</code></strong> (<code>object</code>) (опционально) — Структура ошибки <ul><li><strong><code>code</code></strong> (<code>string</code>) — Код ошибки</li><li><strong><code>message</code></strong> (<code>string</code>) — Сообщение об ошибке</li><li><strong><code>details</code></strong> (<code>array</code>) (опционально) — Детали ошибки</li></ul></li></ul><p><strong>Пример использования:</strong></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># В сценарии</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;validate&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    condition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;example&quot;</span></span></code></pre></div><details><summary>📖 Дополнительная информация</summary><p>Поддерживаемые операторы: <code>==</code>, <code>!=</code>, <code>&gt;</code>, <code>&lt;</code>, <code>&gt;=</code>, <code>&lt;=</code>, <code>in</code>, <code>not in</code>, <code>~</code>, <code>!~</code>, <code>regex</code>, <code>is_null</code>, <code>not is_null</code></p><p><strong>Плейсхолдеры:</strong></p><ul><li>✅ <code>condition: &quot;{_cache.storage_values.tenant_owner|exists} == True and {_cache.storage_values.tenant_owner|length} &gt; 0&quot;</code></li><li>✅ <code>condition: &quot;{_cache.storage_values.tenant_id} == 137&quot;</code></li><li>✅ <code>condition: &quot;{_cache.storage_values.tenant_id} &gt; 100&quot;</code></li></ul><p><strong>Маркеры:</strong></p><ul><li>✅ <code>condition: &quot;$user_id &gt; 100&quot;</code></li><li>✅ <code>condition: &quot;$_cache.storage_values.tenant_owner not is_null&quot;</code></li><li>✅ <code>condition: &quot;$event_type == &#39;message&#39; and $user_id &gt; 100&quot;</code></li></ul></details>`,17)])])}const y=n(l,[["render",d]]);export{u as __pageData,y as default};
